# simple_snowflake_mcp configuration
logging:
  level: INFO
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

server:
  name: "simple_snowflake_mcp"
  version: "0.3.0"

snowflake:
  read_only: true
  default_query_limit: 1000
  max_query_limit: 50000
